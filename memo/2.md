# 

```
Typ.id(v);
```
```
Typ.of(v, a=>a.p.num);
```
```
Typ.of(v, Number);
```
```
Typ.of(v, []); // a=>a.r.ary
Typ.of(v, [Number]); // a=>a.r.ary(a=>a.p.num)
Typ.of(v, [Number,NaN]); // a=>a.r.ary(a=>a.p.num, a=>a.c.nan)
Typ.of(v, [Number]); // a=>a.r.ary(a=>a.p.num)
Typ.of(v, [[]]);    // a=>a.ar.ary(a=>a.ar.ary)
Typ.of(v, [[Number]]); // a=>a.ar.ary(a=>a.ar.ary(a=>a.p.num))
Typ.of(v, [a=>a.r.cls(Date)]); // a=>a.r.ary(a=>a.r.cls(Date))
```
```
Typ.of(v, {});
Typ.of(v, {name:String});
Typ.of(v, {name:String, age:a=>a.p.num});
```

```
Typ.id(v);
Typ.of(v, Number);
Typ.is(v, Number);
Typ.e.of(v, Number);
Typ.e.is(v, Number);
```

* Typ
    * CbFnTyp
    * ConTyp
    * AryTyp
    * ObjTyp


